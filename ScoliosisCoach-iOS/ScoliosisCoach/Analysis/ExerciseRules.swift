import Foundation

struct ExerciseResult {
    let score: Int
    let feedback: String
}

func analyzeExercise(landmarks: [[String: CGFloat]]) -> ExerciseResult {
    // Placeholder: full exercise classification requires per-exercise logic
    ExerciseResult(score: 100, feedback: "Good form detected. Continue regular exercise as prescribed.")
}
